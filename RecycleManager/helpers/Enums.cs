namespace RecycleManager.helpers
{
    public class Enums
    {
        public enum WebAttributes
        {
            LoginSession,
            UserLoginId,
            UserRole,
        }

        public enum Roles
        {
            admin,
            user,
        }
    }
}